return {
	"folke/zen-mode.nvim",
	opts = {
		window = {
			width = 0.6, -- width will be 80% of the screen
			relative = "win", -- position is win
			height = 1, -- height will be 1 line
			resize = true, -- when the window size changes, adjust the text width
			list = true, -- show the list of windows
			backdrop = 0,
		},
	}
}
